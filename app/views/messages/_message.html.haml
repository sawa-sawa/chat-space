.MessageBox{data: {message: {id: message.id}}}
  .Chat_main__message-list__member
    .Chat_main__message-list__member__name
      = message.user.name
    .Chat_main__message-list__member__date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")

  .Chat_main__message-list__message
    - if message.content.present?
      = message.content
    = image_tag message.image.url, class:".Chat_main__message-list__message__image"  if message.image.present?